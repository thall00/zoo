require 'rails_helper'

RSpec.describe Tiger, type: :model do
	describe "in the zoo" do
		it "doesn't do anything yet"
	end
	#expect(100).to eq(Tiger.appetite(default))  # passes if actual == expected
end
